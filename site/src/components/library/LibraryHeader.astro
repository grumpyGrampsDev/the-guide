<section class="library-header">
  <h1>The Library</h1>

  <p class="lead">
    Every shelf in The Guide lives here.
  </p>

  <p>
    Some shelves introduce the posture behind reading Scripture.
    Others focus on prayer, reflection, remembrance, faithful
    walking, and examples from the journey.
  </p>

  <p>
    There is no required order. Begin with the Introduction if
    you're new, or simply browse and return to a shelf whenever a
    particular season calls for it.
  </p>

  <hr />
</section>

<style>
  .library-header {
    text-align: center;

    max-width: 48rem;

    margin: 0 auto 5rem;
  }

  .library-header h1 {
    margin: 0 0 2.5rem;

    font-family: var(--font-ui);
    font-size: clamp(2.75rem, 6vw, 4rem);
    font-weight: 700;
    line-height: 1.1;
  }

  .lead {
    margin-bottom: 1.5rem;

    font-size: 1.4rem;
    color: var(--ink);
  }

  .library-header p:not(.lead) {
    max-width: 42rem;

    margin: 0 auto 1.5rem;

    color: var(--ink-soft);
  }

  .library-header hr {
    width: 20rem;

    margin: 4rem auto 0;

    border: 0;
    border-top: 1px solid var(--border);
  }
</style>