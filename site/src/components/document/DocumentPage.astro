---
import Page from "../layout/Page.astro";
import DocumentNavigation from "../navigation/DocumentNavigation.astro";
import DocumentFooter from "./footer/DocumentFooter.astro";

import type {
  GuideDocument,
  GuideShelf,
} from "../../lib/librarian";

import DocumentBody from "./DocumentBody.astro";

interface Props {
  document: GuideDocument;
  shelf: GuideShelf;
}

const {
  document,
  shelf,
} = Astro.props;

---

<Page>
  <DocumentNavigation
  shelfHref={`/library/${document.shelf}`}
  shelfLabel={`${shelf.name} Shelf`}
/>

  <DocumentBody document={document} />

  <DocumentFooter
    putIntoPractice={document.putIntoPractice}
    recommendedNext={document.recommendedNext}
    relatedReading={document.relatedReading}
  />
</Page>