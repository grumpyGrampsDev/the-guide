---
import { getSection } from "../lib/librarian";

const documents = await getSection("introduction");
---


<h1>Introduction</h1>

<ul>
  {documents.map((doc) => (
    <li>{doc.title}</li>
  ))}
</ul>
