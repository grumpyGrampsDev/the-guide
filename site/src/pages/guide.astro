---
import { getDocument } from "../lib/librarian";

const guideDocument = await getDocument(
  "introduction/purpose-of-the-guide",
);
---

<h1>{guideDocument?.title}</h1>

<pre>{guideDocument?.content}</pre>